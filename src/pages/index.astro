---
import Layout from '../layouts/Layout.astro';
import ProductCard from '../components/ProductCard.astro';
import CategoryCard from '../components/CategoryCard.astro';
import Newsletter from '../components/Newsletter.astro';

import products from '../data/products.json';
import categories from '../data/categories.json';

const featuredProducts = products.filter(p => p.featured).slice(0, 6);
const trendingProducts = products.filter(p => p.trending).slice(0, 4);
---

<Layout title="TheBestOffersAround.com - Discover Today's Best Deals & Offers">
  <div class="space-y-16">
    <section class="hero text-center py-16 bg-gradient-to-b from-complementary/20 to-transparent rounded-lg">
      <h1 class="text-5xl md:text-6xl font-bold mb-6 bg-gradient-to-r from-primary to-complementary bg-clip-text text-transparent">
        Discover Today's Best Deals & Offers
      </h1>
      <p class="text-xl text-gray-300 mb-8 max-w-3xl mx-auto">
        Handpicked products and courses to help you succeed in health, wealth, and personal growth. 
        All offers verified and updated daily.
      </p>
      <div class="flex flex-wrap justify-center gap-4">
        <a href="#featured" class="btn btn-lg">
          Browse Deals
        </a>
        <a href="/categories" class="btn btn-lg bg-complementary hover:bg-complementary/85">
          View Categories
        </a>
      </div>
      
      <div class="flex justify-center gap-8 mt-12 text-center">
        <div>
          <div class="text-3xl font-bold text-primary">1000+</div>
          <div class="text-gray-400">Verified Deals</div>
        </div>
        <div>
          <div class="text-3xl font-bold text-primary">50K+</div>
          <div class="text-gray-400">Happy Customers</div>
        </div>
        <div>
          <div class="text-3xl font-bold text-primary">100%</div>
          <div class="text-gray-400">Trusted</div>
        </div>
      </div>
    </section>

    <section id="featured">
      <div class="flex items-center justify-between mb-8">
        <h2>⭐ Featured Offers</h2>
        <a href="/products" class="text-primary hover:underline">View All →</a>
      </div>
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        {featuredProducts.map(product => (
          <ProductCard {...product} />
        ))}
      </div>
    </section>

    <section>
      <h2 class="mb-8 text-center">Browse by Category</h2>
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
        {categories.map(category => (
          <CategoryCard {...category} />
        ))}
      </div>
    </section>

    <section>
      <div class="flex items-center justify-between mb-8">
        <h2>🔥 Trending Now</h2>
        <a href="/products?filter=trending" class="text-primary hover:underline">View All →</a>
      </div>
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
        {trendingProducts.map(product => (
          <ProductCard {...product} />
        ))}
      </div>
    </section>

    <section>
      <Newsletter />
    </section>

    <section class="bg-gray-800 rounded-lg p-8">
      <h2 class="text-center mb-6">Why Choose TheBestOffersAround?</h2>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        <div class="text-center">
          <div class="text-4xl mb-3">✅</div>
          <h3 class="text-lg font-bold mb-2 text-white">Verified Quality</h3>
          <p class="text-gray-400">Every product is carefully reviewed and tested by our team</p>
        </div>
        <div class="text-center">
          <div class="text-4xl mb-3">💯</div>
          <h3 class="text-lg font-bold mb-2 text-white">Best Prices</h3>
          <p class="text-gray-400">We negotiate exclusive discounts you won't find anywhere else</p>
        </div>
        <div class="text-center">
          <div class="text-4xl mb-3">🔒</div>
          <h3 class="text-lg font-bold mb-2 text-white">Secure & Trusted</h3>
          <p class="text-gray-400">Shop with confidence through our trusted affiliate partners</p>
        </div>
      </div>
    </section>

    <section class="text-center text-sm text-gray-400 border-t border-gray-700 pt-8">
      <p class="mb-2">
        <strong>Affiliate Disclosure:</strong> TheBestOffersAround.com is a participant in various affiliate marketing programs. 
        We may earn a commission when you click links and make purchases through our site, at no additional cost to you.
      </p>
      <p>
        We only recommend products and services we believe will add value to our readers.
      </p>
    </section>
  </div>
</Layout>
